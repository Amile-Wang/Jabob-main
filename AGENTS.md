# Repository Agent Instructions

## GitNexus（仅显式启用）

- 默认不要运行 GitNexus CLI、MCP 查询、索引、影响分析或变更检测。
- 只有用户在当前请求中明确要求使用 GitNexus 时，才允许调用相关能力。
- 日常代码理解、调试、修改和验证优先使用普通工作区搜索、文件读取、语言服务、编译与测试工具。
- 保留现有 GitNexus 索引和技能文件，便于用户以后按需手动启用；不要因普通任务自动刷新或改写它们。
